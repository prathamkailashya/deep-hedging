# =====================================================================
# Deep Hedging (pass 5) — dependencies
# The CORE block is all that is needed to reproduce every pass-5 result
# (walk-forward, bootstrap, band overlay, cross-market, in-distribution).
# =====================================================================

# --- CORE (required) ---
torch>=2.1.0
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
yfinance>=0.2.28      # live SPY / ^NSEI OHLCV for the walk-forward
tqdm>=4.65.0
pytest>=7.4.0

# --- OPTIONAL (only for figure generation / legacy scripts; not needed
#     for the numeric results and intentionally excluded from Colab) ---
# matplotlib>=3.7.0
# seaborn>=0.12.0
# statsmodels>=0.14.0
# optuna>=3.2.0            # hyper-parameter search (jaws_research)
# stable-baselines3>=2.0.0 # SAC-CVaR baseline only
# gymnasium>=0.28.0
